import React, { ReactElement } from 'react';
import { IBrandsPageProps } from '../../Brands.types';

export const BrandsTable = (props: { pageState: IBrandsPageProps }): ReactElement => {
    // props
    const {} = props;

    return <div></div>;
};
